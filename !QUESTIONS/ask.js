//  WHICH ONE CORRECT?????
      browserHistory.push("/users/:id/dashboard");
      browserHistory.push(`/users/${this.props.params.id}/dashboard`);

// IS THE Nav.js FILE

//  In the INDEX.js
     should the routes be  user/:id for all
     should I do a pop-up for each edit that will take text?
     this.state = {


  // IN wireframe.js
// this is supposed to represent a URL, is this correct?
         wireframe: " ",
     }
 }


should the whole Proj dash be the parent with a ll the others being the child
 For any field that is just going to allow data to be entered, is that a dumb Child comp
